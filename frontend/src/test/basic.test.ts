import { describe, it, expect } from 'vitest'

describe('Vitest Basic Tests', () => {
  it('should pass', () => {
    expect(1 + 1).toBe(2)
  })

  it('should be true', () => {
    expect(true).toBe(true)
  })
})
