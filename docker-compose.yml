version: '3.8'

services:
  mindmap:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mindmap-app
    ports:
      - "9000:9000"
    volumes:
      # 持久化数据库数据
      - ./data:/app/backend/data
    restart: unless-stopped
