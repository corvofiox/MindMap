version: '3.8'

services:
  mindmap:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mindmap-app
    ports:
      - "8173:3000" # HTTP + WebSocket 服务器（共享端口）
    volumes:
      # 持久化数据库数据
      - ./data:/app/backend/data
    restart: unless-stopped
