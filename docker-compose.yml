version: '3.8'

services:
  mindmap:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mindmap-app
    ports:
      - "8001:3000"  # API 服务器
      - "3001:3001"  # WebSocket 服务器
    volumes:
      # 持久化数据库数据
      - ./data:/app/backend/data
    restart: unless-stopped
