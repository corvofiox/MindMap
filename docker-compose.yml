version: '3.8'

services:
  mindmap:
    build: .
    ports:
      - "8001:3000"
      - "3001:3001"
    environment:
      - NODE_ENV=production
    volumes:
      - ./data:/app/backend/data
    restart: unless-stopped
